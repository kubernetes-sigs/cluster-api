apiVersion: cluster.x-k8s.io/v1beta1
kind: Cluster
metadata:
  name: quick-start-cluster
  namespace: default
spec:
  topology:
    class: quick-start
    controlPlane:
      replicas: 1
    version: 1.27.1
    workers:
      machineDeployments:
      - class: default-worker
        name: md-0
        replicas: 1
      # machinePools:
      # - class: default-mp
      #   name: mp-0
      #   replicas: 1