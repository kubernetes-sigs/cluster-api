apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: controller-manager
  namespace: system
spec:
  template:
    spec:
      containers:
      - image: k8s.gcr.io/cluster-api/cluster-api-controller:v0.1.5
        name: manager
