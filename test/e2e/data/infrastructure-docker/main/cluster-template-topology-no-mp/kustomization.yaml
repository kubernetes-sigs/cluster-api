resources:
  - ../bases/cluster-with-topology.yaml
  - ../bases/crs.yaml

patches:
- path: remove-topology-workers-mp.yaml
  target:
    group: cluster.x-k8s.io
    version: v1beta2
    kind: Cluster
